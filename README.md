# Part 1: Data Audit, EDA & Business Understanding

## D2C Customer Churn Intelligence — Capstone Project

### Overview
This repository contains the data audit, exploratory data analysis (EDA), and business understanding work for a D2C personal-care brand's churn intelligence system. The goal is to understand the raw data, identify quality issues, and surface early churn signals before any predictive model is built.

---

## Repository Structure

```
part1-data-audit-eda/
├── README.md
├── eda_audit.ipynb              # Full EDA notebook with analysis, validation, and charts
├── data_quality_report.md       # Data quality audit and recommended treatments
├── business_memo.md             # Business-facing summary with evidence-based insights
├── requirements.txt             # Python dependencies
├── charts/                      # Saved chart outputs (generated by notebook)
└── outputs/                     # Intermediate CSVs and exported summaries
```

---

## Dataset

Download the dataset from:
[Google Drive Dataset Package](https://drive.google.com/drive/folders/1PmLapJI1VSDgvl_AxARNKwM1MCd3WFX0?usp=sharing)

Place all downloaded CSV files inside a `data/` folder in the project root:

```
part1-data-audit-eda/
└── data/
    ├── customers.csv
    ├── orders.csv
    ├── support_tickets.csv
    ├── web_events_snapshot.csv
    ├── churn_labels.csv
    ├── intervention_history.csv
    └── rfm_modeling_snapshot.csv
```

---

## Setup Instructions (Tested on Python 3.11.9)

### 1. Clone the Repository
```bash
git clone https://github.com/engrsanjiv-ai/part1-data-audit-eda.git
cd part1-data-audit-eda
```

### 2. Create a Virtual Environment (Recommended)
```bash
py -3 -m venv .venv
# Windows PowerShell
.\.venv\Scripts\Activate.ps1
# Windows CMD
.\.venv\Scripts\activate
# macOS / Linux
source .venv/bin/activate
```

### 3. Install Dependencies
```bash
pip install -r requirements.txt
```

### 4. Launch the Notebook
```bash
jupyter notebook eda_audit.ipynb
```

Run the full notebook from top to bottom. Charts are saved to `charts/`; summary exports appear in `outputs/`.

---

## Key Outputs

| File | Description |
|------|-------------|
| `eda_audit.ipynb` | Full analysis: schema checks, diagnostics, visualizations, and export logic |
| `data_quality_report.md` | Structured audit of dataset issues and remediation recommendations |
| `business_memo.md` | Business-facing summary with data-backed action items |
| `charts/` | Visualizations generated by notebook analysis |
| `outputs/merged_master.csv` | Joined master dataset produced by the notebook |

---

## Findings Summary

- **Churn rate** in the labeled dataset is 46.96% churned vs 53.04% retained.
- **Recency** is the strongest signal: customers with last order ≤60 days before snapshot churn at 18.95%, while customers with last order >60 days churn at 72.30%.
- **Support ticket status** is not clearly validated in the current CSVs; unresolved-ticket claims require richer ticket-state history.
- **Discount usage** is very common: 2,356 of 2,400 customers with orders have a discount-usage rate >70%, and churn rates are similar across high- and low-discount cohorts.
- **Low engagement** is a strong signal: 4+ sessions in 30 days have a churn rate of 36.11%, while 0 or 1 session have churn rates above 66%.

See `business_memo.md` for prioritized recommendations and evidence-based next steps.

---

## Notes
- The notebook uses **relative paths** and does not rely on hardcoded local directories.
- No API keys, tokens, or credentials are present in this repository.
- Python 3.11.9 is recommended with the dependencies in `requirements.txt`.
